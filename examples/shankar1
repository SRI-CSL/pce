sort Bowl;
const a, b: Bowl;
sort Color;
const plain, choc: Color;
predicate bowl(Bowl) indirect;
predicate cookie(Color) indirect;
add ~bowl(a) or ~bowl(b);
add ~cookie(plain) or ~cookie(choc);
add bowl(a) or bowl(b);
add bowl(a) .69;
add bowl(b) .69;
add ~bowl(a) or cookie(plain) 1.386;
add ~bowl(a) or cookie(choc) .28768;
add ~bowl(b) or cookie(plain) .693;
add ~bowl(b) or cookie(choc) .693;
add cookie(plain);
mcsat_params 5000, 0.01, 20.0, 0.01, 30;
mcsat;